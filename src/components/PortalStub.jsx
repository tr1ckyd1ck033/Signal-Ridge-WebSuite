import React, { useState } from 'react';export default function PortalStub(){const [pwd,setPwd]=useState('');const [ok,setOk]=useState(false);function submit(e){e.preventDefault();if(pwd==='s3cur3')setOk(true);else alert('Access denied')}return (<section className='container mx-auto py-12 section'><div className='watermark'></div><h2 className='text-2xl mb-4'>Client Portal (restricted)</h2>{ok?(<div><p>Secure files and active cases will appear here. Use encrypted transfers for sensitive uploads.</p></div>):(<form onSubmit={submit} className='max-w-md'><label className='block mb-2'>Portal password<input className='w-full mt-2 p-2 bg-transparent border border-srgray rounded' value={pwd} onChange={e=>setPwd(e.target.value)} /></label><button className='cta cta-primary'>Enter Portal</button></form>)} </section>)}